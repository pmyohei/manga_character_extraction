# CLAUDE.md

このファイルは Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイドです。
**この指示は既定の挙動より優先されます。記載どおりに従ってください。**

## このリポジトリについて

ソフトウェアプロジェクトではない。コード・ビルド・テストは存在しない。
**漫画の画像からセリフを抽出してテキスト化する**ための作業場である。

- 入力: `0_漫画/` 配下のページ画像
- 成果物:
  1. `1_セリフデータ/` 配下に出力する巻ごとの `.txt`
  2. 抽出対象とした画像ファイルのリネーム

派生作業として、`1_セリフデータ/` のテキストを形態素解析し単語単位に分割する
**単語抽出**がある（→「単語抽出（2_単語抽出/）」）。

## ディレクトリ構成

```
0_漫画/<作品名>/<巻ディレクトリ>/<画像ファイル>
1_セリフデータ/<作品名>/<作品名>_<巻数>巻.txt
2_単語抽出/<作品名>/単語_<作品名>_<巻数>巻.txt
```

### 0_漫画/（入力）

- 直下に作品ごとのディレクトリ、その下に**巻ごとのサブディレクトリ**を作り画像を置く。
  - 巻ディレクトリ名は `1巻`, `2巻` の形式。
  - **どの話が何巻に属するかはこのフォルダ構成で表現する。** Claude は巻を推測しない。
    画像が置かれている巻ディレクトリがその画像の巻。
- ページ順の唯一の手がかりは元ファイル名末尾の `_<数字>`。
  - 番号は途中から始まることがある（例: `0_漫画/ONEPIECE/1巻/` の画像は
    `_5` から始まり、`_5` が第1話の扉絵）。
  - 要求された範囲は、ユーザーが既に処理済みのページを意図的に飛ばすことがある。
- 画像は Read ツールで開く（画像を描画する）。読み取り方法は
  「画像の読み取り方法（精度優先）」に従う。

### 1_セリフデータ/（出力）

- 作品名でディレクトリを作成し、その中に**巻ごとの `.txt`** を出力する。
- ファイル名: `<作品名>_<巻数>巻.txt`
  - 例: `1_セリフデータ/ONEPIECE/ONEPIECE_1巻.txt`, `1_セリフデータ/ONEPIECE/ONEPIECE_2巻.txt`

### 作品名トークン

- ファイル名・txt名・ディレクトリ名で使う `<作品名>` は、半角スペースを除いた形にする。
  - 例: `ONE PIECE` → `ONEPIECE`

## セリフ抽出ルール

- **指定された画像のみを対象にする。** 範囲外・処理済みのページには触れない。
- **セリフとナレーション（キャプション枠）のみを抽出する。** 発話者の情報は不要。
  - キャラクター名札枠（例:「村の少年 モンキー・D・ルフィ」「海賊頭 "赤髪の"シャンクス」）は
    発話者情報 → 抽出しない。
  - ナレーション／キャプション枠はセリフとしてページ内の出現順に含める。
- **効果音・オノマトペは抽出しない**（ドン、バシャ、ガチャ、びよーん 等）。
  - ただし言葉として読める叫び（「助けて」「ちくしょう」「銃!!!」等）はセリフとして残す。
  - 書き文字の絶叫（言葉になっていないもの）は残さない。
- **ルビ**: 特殊な読みをするものだけ、括弧付きで出力する。
  - 例: 銃（ピストル）、海賊王（ロジャー）
  - 通常の振り仮名（海賊＝かいぞく 等）は出力しない（ベーステキストのみ）。
- **扉絵**: 話数ロゴ・サブタイトル（例:「第1話 ROMANCE DAWN ―冒険の夜明け―」）は抽出しない。
  扉絵上のナレーション枠はセリフとして抽出する。
- パネル内の記号・強調（!!、——、…）は表記どおりに残す。
- **読み取れない箇所がある場合、文脈から予測して文字を補ってはならない。**
  - 判読できない文字・単語は憶測で埋めず、そのまま欠落させずに扱いを保留する。
  - 該当箇所は `1_セリフデータ/` への出力後、一覧にしてユーザーに必ず提示する
    （対象画像・話数・ページ数・該当セリフの前後関係が分かる形で）。

## 出力フォーマット（.txt）

- 1つの `.txt` = 1巻。
- 話の区切り: `■第<話数>話`
- ページの区切り: `■P.<ページ数>`
  - **ページ数は話ごとに 1 から振り直す。** 扉絵はその話の1ページ目（`■P.1`）。
- セリフは画像内の出現順（おおむね右上→左下、コマ順）に1行ずつ。
- セリフのないページは `■P.<n>` の次行に `（セリフなし）` と書く。
- 既存の `.txt` に追記する場合は、話番号・ページ番号の順序を保つ。

例:

```
■第1話
■P.1
富・名声・力 かつてこの世の全てを手に入れた男 海賊王（ゴールド・ロジャー）
彼の死に際に放った一言は 全世界の人々を海へ駆り立てた
おれの財宝か？欲しけりゃくれてやるぜ…
探してみろ この世の全てを そこに置いてきた
世は……大海賊時代を迎える
■P.2
（セリフなし）
```

## 抽出対象画像のリネーム

抽出が終わったら、対象とした画像ファイルを次の形式にリネームする（拡張子は元のまま）。
リネーム後の画像は元の `0_漫画/<作品名>/<巻ディレクトリ>/` に置いたままにする。

- **通常ページ**: `<作品名>_<巻数>巻_<話数>話_<ページ数>P.<拡張子>`
  - ページ数は話ごとに 1 から。扉絵は `_1P`。
  - 例: `ONEPIECE_1巻_1話_1P.jpg`, `ONEPIECE_1巻_1話_2P.jpg`
- **おまけページ**（話と話の間のページ。**扉絵ではない**ことに注意）:
  `<作品名>_<巻数>巻_<話数>話_おまけページ<連番>.<拡張子>`
  - 話数は**直前の話番号**。
  - 複数枚あるときは連番（1, 2, 3, …）を付ける。
  - 例: `ONEPIECE_1巻_1話_おまけページ1.jpg`, `ONEPIECE_1巻_1話_おまけページ2.jpg`

## 画像の読み取り方法（精度優先）

**このプロジェクトでは効率よりも読み取り精度を優先する。** 1ページずつ、以下の
2パス方式で読み取ること。複数ページをまとめて Read し、記憶を頼りに書き起こす
方法は精度が落ちるため行わない。

- **パス1（書き起こし）**: 1ページを Read で開いた直後、画像が直接見えている
  状態のまま、吹き出しごとに一字一句書き起こす。「後でまとめて書く」ことは
  絶対にしない。
- **パス2（照合）**: パス1の書き起こし文を見ながら同じページをもう一度 Read し、
  1行ずつ実際の文字と一致しているか照合する。一致しない語・確信が持てない語は
  ここで初めて具体的に特定できる。
- 一致しない・確信が持てない語は、文脈から意味を逆算して埋めない
  （→「セリフ抽出ルール」の「読み取れない箇所がある場合」に従う）。
- 小さい文字・スピード線と重なった文字は、周辺コマとの位置関係だけでなく、
  吹き出しの形と尻尾（どのキャラに向いているか）を明示的に確認してから
  読み順・話者を決める。
- 1ターンに詰め込むページ数を絞る（目安: 10ページ程度で区切る）。ページ数が
  多いほど後半で照合が甘くなりやすい。区切りごとにパス1・パス2を完結させてから
  次の区切りに進む。

## 作業手順

1. 対象画像を「画像の読み取り方法（精度優先）」に従って1ページずつ読み取る。
2. ページごとに抽出ルールに従ってセリフを書き起こす。
3. `1_セリフデータ/<作品名>/<作品名>_<巻数>巻.txt` に出力する
   （新規なら作成、既存なら話番号・ページ番号順を保って追記）。
4. 対象とした画像ファイルをリネームする。

## 単語抽出（2_単語抽出/）

`1_セリフデータ/` のセリフテキストを形態素解析し、単語単位に分割して出力する派生作業。
セリフ抽出とは独立した作業で、ユーザーが対象（作品・巻・話）を指定して依頼する。
**目的は単語の出現頻度分析。**

ルールはすべて `2_単語抽出/` 配下のファイルに記録している。作業前に必ず読むこと。

- **共通ルール**: `2_単語抽出/単語分割ルール_共通.md`
  （入力・抽出モード・抽出ルール・出力フォーマット・作業手順・全作品共通の確定判断）
- **作品別ルール**: `2_単語抽出/<作品名>/単語分割ルール_<作品名>.md`
  （その作品固有の複合語・固有名詞の分割粒度などの確定判断。1作品1ファイル）

新しく確定した判断は、全作品に効くものは共通ファイル、作品固有のものは作品別ファイルへ追記する。
